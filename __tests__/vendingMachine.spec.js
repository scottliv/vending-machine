const vendingMachine = require("../lib/vendingMachine");

define("vendingMachine", () => {
  define("restock", () => {});
  define("change", () => {});
  define("sale", () => {});
  define("inventory", () => {});
});
